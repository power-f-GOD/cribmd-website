import { PageHeader } from 'src/components/shared';

const Header = (): JSX.Element => {
  return <PageHeader headerText="Gallery" rider="See our achievements and picture displays!" />;
};

export default Header;
